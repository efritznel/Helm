apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-helm-project.fullname" . }}
  labels:
    {{- include "my-helm-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-helm-project.selectorLabels" . | nindent 4 }}
